+ install nodejs 
#https://nodejs.org/dist/v12.16.1/node-v12.16.1-linux-armv7l.tar.xz

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.35.2/install.sh | bash
source ~/.bashrc
nvm install v12

#sudo does not preserve your $PATH environment variable for security reasons. Instead, it gets replaced with a secure_path defined in /etc/sudoers
#add path in this file to run with sudo

sudo visudo

# or sudo

sudo ln /home/pi/.nvm/versions/node/v12.16.1/bin/node /usr/local/bin/node
sudo ln /home/pi/.nvm/versions/node/v12.16.1/bin/npm /usr/local/bin/npm

+ install package
sudo apt-get install bluetooth bluez libbluetooth-dev libudev-dev git build-essential wget curl python

+ setup command



